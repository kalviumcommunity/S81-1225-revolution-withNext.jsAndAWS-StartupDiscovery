/**
 * Custom Hooks Barrel Export
 *
 * Provides convenient imports for all custom hooks:
 * import { useAuth, useUI } from "@/hooks";
 */

export { useAuth } from "./useAuth";
export { useUI } from "./useUI";
